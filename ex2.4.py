'''Q4. Define the following conditions for validating a string as a valid
password. The string should have a minimum of 8 characters and maximum 20
characters.It can contain alphabets(upper and lower case), digits(0 to 9) and
the following special characters only (*, $, #). The password should contain a
minimum of one uppercase letter, one lowercase letter, two digits and only one
of the special characters. The two digits should not be in successive
positions. Accept strings as input and print if they are valid passwords or
not.'''
def valid_password():
    strin=input("Enter the password ")
    if len(strin)<8 || len(strin)>20:
        print("Invalid password ")
    elif:
        
